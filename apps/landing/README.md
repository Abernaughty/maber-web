# landing

The [maber.io](https://maber.io) landing page — a small SvelteKit site that links out to the other apps in this monorepo.

## Development

```bash
pnpm install
pnpm dev
```

Other scripts: `pnpm build`, `pnpm check`, `pnpm lint`.
